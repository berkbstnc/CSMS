@model IEnumerable<CSMS.Models.Service.Car>

@{
    ViewBag.Title = "Car List";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@using (Html.BeginForm())
{
    <div class="row">
        <div class="col">
            Search Car
            <input type="text" class=" form-control" name="search" />
        </div>
    </div>
    <br />
    <div class="col">
        <input type="submit" class="btn btn-secondary" value="Search" />
    </div>
    <br />


}


<hr />
<table class="table table-sm table-responsive">
    <thead>
        <tr>
            <th>Plate</th>
            <th>Brand</th>
            <th>Model</th>
            <th>Year</th>
            <th>Type</th>
        </tr>
    </thead>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Plate</td>
            <td>@item.Name</td>
            <td>@item.Model</td>
            <td>@item.Year</td>
            <td>@item.Type</td>
            <td>
                @Html.ActionLink("Create Fault Record", "OrderCreate", new { customerid = item.CarId }, new { @class = "btn btn-sm btn-success" })
            </td>
        </tr>
    }
</table>
